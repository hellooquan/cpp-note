# 父类地址硬转成子类指针之后,子类独有的函数能调吗

一句话:**能编译、能调进去,但函数里的 `this` 指向的不是子类对象** —— 只要函数体里碰了子类独有的东西(新增的数据成员、子类独有的虚函数),结果就是越界读写 / 乱值 / 崩溃。

(这是第 18 节 3.2「`static_cast` 不看真身」的展开)

## 一、三段代码,三种结果

### 1. 子类独有的普通函数

```cpp
#include <iostream>
using namespace std;

class Base
{
public:
    int x = 1;
    virtual void who() { cout << "Base::who 被调用, x = " << x << endl; }
    virtual ~Base() {}
};

class Derived : public Base
{
public:
    int y = 2;

    void onlyDerived()      // 子类独有:只打印,不碰子类新增成员
    {
        cout << "Derived::onlyDerived 被调用了, x = " << x << endl;
    }

    void touchY()           // 子类独有:要读子类新增的成员 y
    {
        cout << "Derived::touchY 读到的 y = " << y << endl;
    }

    void who() override     // 覆盖基类的虚函数
    {
        cout << "Derived::who 被调用, y = " << y << endl;
    }
};

int main()
{
    Base b;
    b.x = 100;
    Base *p = &b;

    Derived *sp = static_cast<Derived *>(p);   // 父类地址硬转成子类指针

    cout << "&b = " << (void *)&b << ", sp = " << (void *)sp << endl;

    sp->onlyDerived();   // 子类独有函数:能调用吗?
    sp->touchY();        // 子类独有函数:里面读子类新增成员
    sp->who();           // 虚函数:走的是谁的版本?
}
```

编译运行:`g++ -Wall -std=c++17 -o d1 d1.cpp && ./d1`

实测输出:

```
&b = 0x7fff334689a0, sp = 0x7fff334689a0
Derived::onlyDerived 被调用了, x = 100
Derived::touchY 读到的 y = 0
Base::who 被调用, x = 100
```

- 第一行:同一个地址,硬转没搬动任何东西,只是把"我是 `Base`"改口成"我是 `Derived`"
- 第二行:子类独有的函数**真的被调用了**,而且正常打印 —— 因为它只用了基类成员 `x`
- 第三行:它读 `y` 时读到 `0`,这个 `0` 不是 `y` 的值,是从 `b` 后面那块内存里抓来的;换成别的环境就是别的垃圾值
- 第四行:调 `who()` 走的是 **Base 的版本**(真身是 Base,虚表就是 Base 的),`override` 白写了

### 2. 子类独有的函数里,碰子类新增的数据成员

```cpp
#include <iostream>
using namespace std;

class Base
{
public:
    int x = 1;
    virtual ~Base() {}
};

class Derived : public Base
{
public:
    long long pad[8] = {};   // 子类新增一大块数据

    void writeInto()         // 子类独有函数:往子类新增成员里写
    {
        pad[3] = 7;
    }
};

int main()
{
    Base *p = new Base;
    Derived *sp = static_cast<Derived *>(p);

    sp->writeInto();   // 写进缓冲区之外
    cout << "写完了(没报错)" << endl;

    delete p;
}
```

编译运行(要 ASAN 才看得见):`g++ -Wall -std=c++17 -fsanitize=address -g -o d2 d2.cpp && ./d2`

实测输出(节选):

```
ERROR: AddressSanitizer: heap-buffer-overflow
WRITE of size 8 at 0x796f788e0038 thread T0
    #0 Derived::writeInto() d2.cpp:18
0x796f788e0038 is located 24 bytes after 16-byte region [0x796f788e0010,0x796f788e0020)
```

那块内存只有 **16 字节**(`Base` 的大小),按 `Derived` 的布局去写 `pad[3]`,写到 24 字节之外去了。

### 3. 子类独有的「虚函数」

```cpp
#include <iostream>
using namespace std;

class Base
{
public:
    int x = 1;
    virtual void who() { cout << "Base::who" << endl; }
    virtual ~Base() {}
};

class Derived : public Base
{
public:
    int y = 2;

    virtual void extra()   // 子类独有的虚函数:Base 的虚表里没有这一格
    {
        cout << "Derived::extra 被调用, y = " << y << endl;
    }
};

int main()
{
    Base b;
    Base *p = &b;

    Derived *sp = static_cast<Derived *>(p);
    cout << "准备调用子类独有的虚函数 extra() ..." << endl;
    sp->extra();
    cout << "调用返回了" << endl;
}
```

实测输出:

```
准备调用子类独有的虚函数 extra() ...
Segmentation fault(退出码 139)
```

`extra()` 里第一句 `cout` 都没打印出来 —— 崩在"查虚表拿函数地址"那一步:`Base` 的虚表里压根没有 `extra` 这一格,查出来的是个垃圾地址,跳过去就是段错误。这一档**必崩**,而且每次跑都崩。

## 二、结果对照表

| 子类独有的函数里干了什么 | 能调用吗 | 实际结果 |
| --- | --- | --- |
| 只用基类成员(如 `x`) | 能 | 正常执行,看不出任何问题 ← 最容易骗人的一档 |
| 碰子类新增的数据成员(如 `y`、`pad`) | 能 | 越界读写:读到垃圾值,写进去会踩坏别的内存(ASAN 报 `heap-buffer-overflow`) |
| 是子类独有的虚函数 | 能 | 虚表里没那一格 → `Segmentation fault`,退出码 139 |

## 三、为什么"能调"

编译器分两步,只看 `sp` 的**静态类型**:

- **编译期**:`sp` 声明成 `Derived *`,所以 `Derived` 里独有的名字全可见,`sp->onlyDerived()` / `sp->touchY()` / `sp->extra()` 语法上都合法,一个警告都没有
- **运行期**:真正干活时,数据成员的偏移量按 `Derived` 的布局算、虚函数按 `Derived` 的虚表查格子 —— 可是这块内存只有 `Base` 那么大,两样都越界了

所以"能编译 + 能调用"只是骗过了编译器,救不了对象;`static_cast` 也**不做任何真身检查**(它只看两边是不是同一条继承链上的类型)。

## 四、那正确的做法是什么

想从父类指针拿到子类独有功能,先查真身:`dynamic_cast`。

```cpp
#include <iostream>
using namespace std;

class Base
{
public:
    int x = 1;
    virtual void who() { cout << "Base::who" << endl; }
    virtual ~Base() {}
};

class Derived : public Base
{
public:
    int y = 2;
    void onlyDerived() { cout << "Derived::onlyDerived" << endl; }
};

int main()
{
    Base b;
    Base *p = &b;

    Derived *sp = dynamic_cast<Derived *>(p);   // 带真身检查的转型

    cout << "dynamic_cast 结果: " << (sp ? "成功" : "失败(nullptr)") << endl;

    if (sp != nullptr)      // 检查过再用
        sp->onlyDerived();
    else
        cout << "真身不是 Derived, 不给调用" << endl;
}
```

实测输出:

```
dynamic_cast 结果: 失败(nullptr)
真身不是 Derived, 不给调用
```

真身是 `Derived` 时它给你指针,随便调子类独有函数都是安全的;真身是 `Base` 时给 `nullptr`,`if` 一判就知道不能往里调。

**更常见的正道是不转型**:把接口在基类里声明成 `virtual`、子类里 `override`,直接 `p->who()` 就会自动调到子类版本 —— 多态本来就是干这个的,不需要你硬转。

## 五、坑

- 别拿"能跑通"当"没问题":只碰基类成员的那一档是真的正常跑,危险在它会让这次侥幸过关,下次换个函数就炸
- 子类独有的虚函数被硬转后调用 → **必崩**,退出码 139(`SIGSEGV`),连函数体第一句都不执行
- 子类**覆盖**了基类虚函数,硬转后调也**不会**到子类版本:对象真身是 `Base`,虚表就是 `Base` 的(实测打印 `Base::who`)
- 地址相同不代表身份相同:单继承下 `&b` 和硬转出来的 `sp` 是一个地址,但对象还是那个 `Base`
- 想确认"这个父类指针到底能不能当子类用" → `dynamic_cast` 查真身:指针版失败给 `nullptr`,引用版失败抛 `std::bad_cast`(第 18.3)
- 退出码速查:`SIGSEGV` = 139、`SIGABRT` = 134、`SIGPIPE` = 141、被 `timeout` 杀掉 = 124
